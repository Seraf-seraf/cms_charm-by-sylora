# Задачи по ТЗ

## Выполнено

| Итерация | Задача | Репозиторий | Результат | Ограничения | Проверки | Commit |
|---|---|---|---|---|---|---|
| Перенос и переустановка Почты России и Метрики | Перенести локальные изменения в source repositories, пересобрать архивы и переустановить расширения локально и на production | `charmbysylora`, `opencart-russian-post-delivery`, `opencart-yandex-metrica-consent` | Два изменённых файла Метрики перенесены в её source repository; архивы обоих расширений пересобраны; локально и на production старые файлы удалены и 18 файлов установлены заново; production-настройки сохранены, оба расширения включены | OpenCart `Uninstall` не выполнялся; таблицы, настройки и данные не удалялись; CSP allowlist остаётся в CMS, так как не относится к исходникам Почты России; у Почты России source diff отсутствует | Архивы: `unzip -t` успешно; checksum 18 production-файлов совпал; Почта России PHPUnit: 2 теста, 4 assertions; Метрика Node syntax: успешно; production storefront/JS: HTTP 200; настройки: Метрика 7, Почта России 13, status `1` | не создавался |
| P1 review fixes CSP и strict mode | Вернуть CSP-доступ Почте России и SmartCaptcha, устранить необъявленные переменные в cart/checkout | `charmbysylora` | Origins Почты России и SmartCaptcha разрешены в `script-src` и `frame-src`; переменные формирования доставки и индексы циклов объявлены локально | Только замечания P1, без расширения CSP и изменения интеграций | PHPUnit: 11 тестов, 165 assertions; PHPStan: без ошибок; PHP-CS-Fixer: без исправлений; Node syntax: 2 файла | текущий коммит |
| CSP nonce hardening и reports | Убрать executable inline scripts и `on*` с активных storefront-маршрутов, завершить CSP allowlist, закрепить CDEK и добавить безопасные CSP reports | `charmbysylora` | Enforce CSP использует уникальный nonce каждого response, `script-src-attr 'none'` и не содержит `unsafe-eval`; JSON-LD/AJAX JSON config получают nonce; storefront/checkout/cart JS вынесен во внешние файлы с конфигурацией через JSON/`data-*`; CDEK закреплен на `3.10.4` с SRI; report endpoint ограничивает частоту и сохраняет только директиву и origins | `style-src 'unsafe-inline'` сохранен для runtime styles CDEK/Yandex Maps и существующего storefront; неактивные legacy payment/account шаблоны вне проверенного набора маршрутов продолжают защищаться nonce; локально CDEK, SmartCaptcha и payment-service отключены | PHPUnit: 11 тестов, 165 assertions; PHPStan: без ошибок; PHP-CS-Fixer: 0 исправляемых из 2 входящих в project finder файлов; Node syntax: 7 файлов; HTTP-аудит: 0 handlers/inline scripts/JSON-LD без nonce; headless Chrome и checkout guest AJAX: 0 runtime/CSP errors; CSP report: 204 без cookie, URL очищены до origin; CDEK SHA-384 совпал | текущий коммит |
| CSP enforce после локальной проверки интеграций | Ввести CSP сразу в enforce-режиме после локальной проверки активных интеграций | `charmbysylora` | Добавлен request nonce, enforce-заголовок и автоматическая передача nonce во все Twig views | Legacy inline styles и event handlers временно разрешены отдельными директивами; локально активна только тестовая Метрика | Метрика до и после CSP: 3 теста, 27 assertions; CSP contract: 3 теста, 15 assertions; PHPStan: без ошибок; PhpCsFixer: без изменений; полный доменный запуск блокируется дубликатом класса, изолированно падают 2 существующих UI-файла | не создавался |
| Payment lifecycle и остатки | Списывать остаток только после подтвержденного `succeeded`, блокировать опасную конфигурацию и повторные callback | `charmbysylora`, `payment-service` | Единая policy запрещает `pending`/`failed`/`canceled`/`refunded` в processing/complete и требует `succeeded` в этих списках; проверки стоят в admin, выборе метода, `confirm`, callback и непосредственно перед `addOrderHistory`; callback sender использует status snapshot outbox-события | OpenCart остается владельцем order/stock lifecycle; payment-service не управляет заказами и остатками; без `Uninstall`, удаления таблиц и переобработки сохраненных данных | PHPUnit: 5 тестов, 19 assertions; PHPStan: без ошибок; PhpCsFixer: без изменений; `go test ./...`, `go vet ./...`, build: успешно; production заказ #26: `pending` 17 и остаток 1, `succeeded` 18 и остаток 0, повторный succeeded без повторного списания, `refunded` 11 и остаток 1 | не создавался |
| Trusted reverse proxy HTTPS | Учитывать HTTPS за trusted reverse proxy, не доверять клиентскому `X-Forwarded-Proto` | `charmbysylora` | Bootstrap единожды нормализует `HTTPS`; forwarded-заголовки принимаются только при точном совпадении `REMOTE_ADDR` с `TRUSTED_PROXIES`; header/home используют только нормализованное значение | `TRUSTED_PROXIES` — массив точных IP; пустой или некорректный список fail-closed; неоднозначные proto-цепочки отклоняются | PHPUnit: 6 тестов, 12 assertions; PHPStan: без ошибок; PhpCsFixer: без изменений | не создавался |
| Security headers | Добавить Strict-Transport-Security, X-Content-Type-Options, Referrer-Policy и Permissions-Policy | `charmbysylora` | В `catalog/admin` startup добавлены единственные ответы-ответы безопасности: HSTS (`max-age=31536000`), `nosniff`, `strict-origin-when-cross-origin`, `Permissions-Policy` с `camera`, `microphone`, `geolocation` как `()` | HSTS добавляется только при HTTPS, без `includeSubDomains` и `preload`; `Permissions-Policy` настроен на запрет трёх API доступа | Проверки не запускались для этого этапа задач; далее `phpstan`/`php-cs-fixer` по изменённым файлам | не создавался |
| E2E-05 — согласия | Привязка `config_account_id`/`config_checkout_id`, серверная обязательность `agree` и проверки контрактов согласий | `charmbysylora` | Проверки на обязательность соглашений валидации переведены на `empty()`, добавлены доменные browser-тесты для отображения и отсутствия/невалидного `information_id`, сохранён мэппинг `config_account_id` и `config_checkout_id` в регистрационных шагах | Миграций не добавлено, `information_id` берётся из существующего хранилища `setting`; маршрут `account/register` остаётся единственным endpointом регистрации | PHPUnit: 7 тестов, 42 assertions (`tests/phpunit/domen/catalog/CheckoutConsentTest.php`); `node --check` для browser-runner; PHP lint для изменённых PHP; PHP-CS-Fixer: выполнен dry-run, без изменений | не создавался |
| E2E-06 | E2E-06 — availability | `charmbysylora` | Добавлен единый `ProductAvailability`; внедрён на карточке, в каталоге, поиске, спецпредложениях и related products; `schema.org` и `can_buy` формируются из централизованного состояния; добавлено совместимое с OpenCart Loader имя класса | Базовая логика `Осталось мало` не переносится, `state` ограничен тремя значениями; `wishlist` и `compare` не менялись | PHPUnit: 4 теста, 5 assertions; PHPStan без ошибок; PhpCsFixer без изменений; локальный HTTP 500 устранён | `abd1c2c`, `aa93ffe` |
| E2E-08 | Вход в аккаунт в desktop/mobile header | `charmbysylora` | Header получает `logged`, login/account URL; гость видит «Войти», клиент — «Личный кабинет»; action доступен в desktop header и mobile menu, имеет `aria-label`, focus state и touch target 44 px | Существующая навигация, search/cart/menu и auth-маршруты не менялись | Responsive browser E2E: 320, 360, 390, 414, 768, 1024, 1440 и zoom 200%; account visibility/touch target подтверждены | `3b6fee0`, `e43f9ca` |
| E2E-09 — audit | HTTPS canonical redirect | production hosting | ISPmanager flags `redirect_http=on`, `redirect_www=from_www`; root HTTP сохраняет path/query и делает один hop, но HTTP `www` делает 3 hop, HTTPS `www` — 2 hop с промежуточным HTTP | Hosting user не имеет read-доступа к vhost, sudo, `nginx -t` и reload; editor vhost доступен только superuser. Изменения production не применялись | `curl` для трёх входов: redirects `1/3/2`; ISPmanager API read-only audit | не создан |
| E2E-10 | Нормализация обратного диапазона цены | `charmbysylora` | Существующая client/server перестановка min/max подтверждена; category canonical теперь использует нормализованный filter query; отрицательные и нечисловые значения сохраняют прежнее правило `0` | Правила фильтрации и pagination не менялись вне normalization | Browser contract: direct URL без JS, canonical `100/900`, invalid/negative `0/0` | `9d7a3f4`, `e43f9ca` |
| E2E-11 | SEO и юридическое наименование | `charmbysylora` | HTML sitemap получил canonical; 404 сохраняет HTTP 404, `noindex,nofollow` и не получает canonical; footer использует `config_sylora_legal_name` с единым fallback | Документы не дублировались; массовая замена БД не выполнялась | PHPUnit contracts sitemap/404/legal; полный доменный пофайловый прогон | `9d7a3f4`, `e9602c4`, `e43f9ca` |
| Theme post-E2E | Карточка, бренд, shipping cards и filters | `charmbysylora` | Card body получил 16 px без изменения object-fit; бренд не переносится; filters одноколоночные; shipping radio остаётся semantic и вся card кликабельна, добавлены selected/focus/disabled/loading/error states | Payment methods не менялись; hover ограничен fine pointer; ниже 420 px цена и min/max переносятся | UI contracts; browser E2E 9 сценариев, 2337 assertions; assets check | `3b6fee0`, `e43f9ca` |
| Почта России | Backend contract, state machine и release archive | `opencart-russian-post-delivery` | `start_location` строится из адреса получателя, `startZip` остаётся индексом отправителя; реализованы 6 состояний, timeout/retry, server validation, idempotent callback, reset по адресу/корзине и восстановление после AJAX; CSS перенесён в разрешённый installer path | Таблицы, настройки, сохранённые ПВЗ и order records не менялись; production API всё ещё отвечает `1011 ILLEGAL_CREDENTIALS` | Composer validate; PHPUnit 7 тестов/32 assertions; PHP lint; Node check; PhpCsFixer 0/4; archive `unzip -t`; SHA-256 `41db0fbbd7c984c8878ba8b0d5d88bd3f08c6709330fde3ff8b828c6e4ea8361` | `911894f`, `29a9344` |
| Яндекс Метрика | Strict CSP config, consent tests и release archive | `opencart-yandex-metrica-consent`, `charmbysylora` | Config перенесён в escaped `data-config`; local JS валидирует данные; `.site-footer__bottom` и asset version `1.0.1` сохранены; production-copy синхронизирована | `eval` и inline handlers не используются; consent cookie остаётся доступна JS, Secure на HTTPS и SameSite=Lax сохранены | Node: 4 теста; syntax check; sync `--check`; archive `unzip -t`; SHA-256 `c294d99324bb4194dc8c616737ff98849a727fd5cf1bb573c9e75e9e13570eea` | `de5e758`, `b779c4c` |
| Payment-service check | Проверить lifecycle/outbox/callback contract | `payment-service` | Подтверждены new payment `pending`, idempotent succeeded outbox, повторный webhook без transition, signing и retry; OpenCart остаётся владельцем stock/order lifecycle | Новых изменений и коммита главным агентом не создавалось; существующий локальный dirty diff сохранён | `gofmt -l` пусто; `go vet ./...`; `go test ./...`; `go build ./cmd/api` | не создан |
| CMS tests и static checks | Unit/integration/UI/browser contracts | `charmbysylora` | Добавлены stock lifecycle, availability, account, shipping, price, sitemap/404 contracts; устаревшие inline-CSP assertions переведены на внешние JS; Chrome profile cleanup стабилизирован | Полный suite одним процессом блокируется существующим дубликатом `CartUiContractTest`, поэтому все 31 файла запускались по отдельности | PHP lint; Node check; PHPStan без ошибок; PhpCsFixer 0/18; build-assets `--check`; sync Метрики `--check`; 29 файлов прошли общим проходом, 2 исправленных browser-файла повторно: 10 тестов/69 assertions | `eea3fe8`, `e43f9ca` |
| Production rollout | Развернуть CMS и расширения, применить конфигурацию и провести smoke | `charmbysylora`, production hosting, payment-service | Через admin installer без `Uninstall` установлены Метрика и Почта России; 20 CMS-файлов и отдельный layout hotfix развёрнуты с checksum verification; settings расширений сохранены; agreements установлены в `16/17`; документ возврата точечно приведён к единому юр. наименованию; кэши очищены; payment-service использует рабочую кассу и после restart остаётся active | Nginx vhost не изменялся: hosting user не имеет read/sudo/`nginx -t`; Russian Post selection блокируется production API `1011`; новый pending-заказ не создавался, потому что SmartCaptcha потребовала визуальную challenge; обход CAPTCHA и денежная операция не выполнялись | Основные routes HTTP 200; headers/cookies/nonce/404/canonical/price normalization подтверждены; Метрика `0` запросов до consent, загружается после consent и не загружается после revoke; SmartCaptcha 5 requests и 2 iframe; CSP/JS errors `0`; payment-service active, 1 listen socket, errors после restart `0` | `c1e4173`, `0da4480`, `c453719`, `de5e758`, `29a9344` |
| Catalog layout hotfix | Всегда складывать внешний category layout в одну колонку | `charmbysylora` | Внешняя строка category page получила scoped-класс; Bootstrap sidebar/content на всех viewport переводятся в `float: none` и `width: 100%`; CSS cache-buster обновлён | Исправление ограничено category page; другие OpenCart layouts не менялись | PHPUnit 6 тестов/66 assertions; PHPStan без ошибок; PhpCsFixer без изменений; production Chrome: `768px` и `1440px` — sidebar/content full-width и stacked; horizontal scroll и browser errors отсутствуют | `c453719`, `cb80d58` |
| Rollback readiness | Проверить восстановление CMS, данных, расширений и регионов | production hosting | Проверены gzip/tar backups БД, предыдущего CMS, extension files/settings, pre-hotfix CMS и exports `country`/`zone`; рабочие и предыдущие release archives сохранены | Сам nginx vhost не сохранён в post-E2E backup из-за прав hosting user; сохранены `.htaccess` и ISPmanager redirect flags. Rollback расширений выполняется загрузкой предыдущего архива без `Uninstall`; таблицы не удаляются. Заказ №25 и правильный stock не откатываются | `gzip -t`, `tar -tzf`; 10 backup-файлов; archive checksums; production settings до/после установки идентичны. Зафиксированы риск параллельной оплаты последнего экземпляра и остаточный `style-src 'unsafe-inline'`; `unsafe-eval` отсутствует | не создан |
| CI assets | Генерировать minified CSS в CI/CD, а не коммитить вручную | `charmbysylora` | `stylesheet.min.css` удалён из Git index и добавлен в `.gitignore`; lint job выполняет реальную asset-сборку и проверяет создание CSS; production deploy сохраняет существующий build-step после checkout | Новый minifier и зависимости не добавлялись; исходником остаётся `stylesheet.css`; `common.min.js` пока остаётся tracked | CI-команда воспроизведена с отсутствующим target: CSS создан и byte-identical; PHPStan без ошибок; PhpCsFixer без изменений | текущий коммит |
