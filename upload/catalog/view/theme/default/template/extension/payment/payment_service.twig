<div class="buttons">
  <div class="pull-right">
    <input type="button" value="{{ button_confirm }}" id="button-confirm" class="btn btn-primary" data-loading-text="{{ text_loading }}" data-payment-confirm="{{ confirm }}" data-payment-request="json" data-payment-error="{{ error_payment }}" />
  </div>
</div>
